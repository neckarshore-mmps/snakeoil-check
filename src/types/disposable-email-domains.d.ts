// Type declaration for `disposable-email-domains` (ships no types).
// Main export is index.json — a flat array of disposable domains.
declare module 'disposable-email-domains' {
  const domains: string[];
  export default domains;
}
